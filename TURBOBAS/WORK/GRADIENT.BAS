 RANDOMIZE TIMER
 DEFINT A-Z
 SCREEN 12
 FOR A = 1 TO 254: FOR X = 1 TO 253
 OUT &H3C7, X + 1: R = INP(&H3C9): G = INP(&H3C9): B = INP(&H3C9)
 OUT &H3C8, X: OUT &H3C9, R: OUT &H3C9, G: OUT &H3C9, B
 NEXT X
 NEXT A
'PICTURE
 PRINT "HELLO WORLD"
'PICTURE
 DO
 WAIT &H3DA, 8
 FOR X = 0 TO 253
 OUT &H3C7, X + 1: R = INP(&H3C9): G = INP(&H3C9): B = INP(&H3C9)
 OUT &H3C8, X: OUT &H3C9, R: OUT &H3C9, G: OUT &H3C9, B
 NEXT X
 IF CP(1) < PL(1) THEN CP(1) = CP(1) + 1
 IF CP(1) > PL(1) THEN CP(1) = CP(1) - 1
 IF CP(2) < PL(2) THEN CP(2) = CP(2) + 1
 IF CP(2) > PL(2) THEN CP(2) = CP(2) - 1
 IF CP(3) < PL(3) THEN CP(3) = CP(3) + 1
 IF CP(3) > PL(3) THEN CP(3) = CP(3) - 1
 R = CP(1)
 G = CP(2)
 B = CP(3)
 OUT &H3C8, 254: OUT &H3C9, R: OUT &H3C9, G: OUT &H3C9, B
 OUT &H3C8, 255: OUT &H3C9, R: OUT &H3C9, G: OUT &H3C9, B
 IF CP(1) = PL(1) THEN
 IF CP(2) = PL(2) THEN
 HM = INT(RND * 3) + 1
 FOR I = 1 TO HM
 PL(I) = INT(RND * 62)
 NEXT I
 END IF
 END IF
 LOOP UNTIL INKEY$ = CHR$(27)
 END

