    CLS
    SCREEN 9
'DRAW
    LINE(0,0)-(639,340),15,B
    RANDOMIZE TIMER
    FOR K=1 TO 22
    X%=INT(RND * 80)
    Y%=INT(RND * 340)
    Z%=INT(RND * 30)
    J%=INT(RND * 15)
    G%=RND * 2
    M%=RND * 2
    FOR IU=1 TO Z%
    CIRCLE(550+X%,Y%),IU,J%,,M%,G%
    NEXT IU
    NEXT K
'DRAW
    COLOR 4
    LOCATE 2,2
    INPUT"ENTER YOU EARTHQUAKE DIRECTORY (ENTER - CURRENT DIRECTORY) ",A$
    IF A$="" THEN GOTO OPS
    ON ERROR GOTO ERRA
    CHDIR A$
    ON ERROR GOTO ERRA
OPS:
    ON ERROR GOTO ERRA
    COLOR 2
    LOCATE ,2
    FILES"*.MDL"
    LINE(0,0)-(0,340),15
    ON ERROR GOTO ERRA
    GOTO CHKS
ERRA:
    COLOR 3
    LOCATE ,2
    PRINT"NO .MDL FILES IN THIS DIRECTORY"
    DELAY 2
    END
CHKS:
    COLOR 12
    PRINT
    LOCATE ,2
    PRINT"NOW YOU MUST ENTER MODEL FILES(IF FILE WILL BE INCORRECT IT'LL NOT WRITE)"
    COLOR 5
    PRINT
    LOCATE ,2
    INPUT"ENTER MODEL FILE FOR PLAYER 1:",L$
    PRINT
    LOCATE ,2
    INPUT"ENTER MODEL FILE FOR PLAYER 2:",K$
'WRITE
      ON ERROR GOTO POL
      OPEN L$ FOR INPUT AS #1
      ON ERROR GOTO POL
      INPUT #1,AQ$
      ON ERROR GOTO POL
      CLOSE #1
      ON ERROR GOTO POL
      OPEN K$ FOR INPUT AS #1
      ON ERROR GOTO POL
      INPUT #1,AB$
      ON ERROR GOTO POL
      CLOSE #1
      ON ERROR GOTO POL
      OPEN"MODEL1.MDL" FOR OUTPUT AS #1
      PRINT #1,AQ$
      CLOSE #1
      OPEN"MODEL2.MDL" FOR OUTPUT AS #1
      PRINT #1,AB$
      CLOSE #1
      COLOR 9
      PRINT
      locate ,2
      PRINT"EVERYTHING IS ALLRIGHT!"
      DELAY 2
      END
POL:
      COLOR 3
      LOCATE ,2
      PRINT"WRONG FILES! OPERATION INTERRUPTED"
      DELAY 2
      END



